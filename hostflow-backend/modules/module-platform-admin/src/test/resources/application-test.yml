spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:hostflow_platform_admin_test;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
